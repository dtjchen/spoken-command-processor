# take commands.txt and generate the training data (start with a labels.txt
# file and then generate the .wav files for each speaker; also generate
# meta.json with whatever constants are needed; place in volumes/)
